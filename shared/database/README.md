# Database

Shared SQLAlchemy database scaffold for PhiStyle OS.

Current scope:

- connection helper;
- declarative base;
- session factory;
- initial model definitions;
- no ingestion;
- no investment logic.

The default database is PostgreSQL through `DATABASE_URL`.

