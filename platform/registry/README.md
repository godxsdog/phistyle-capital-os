# App Registry

OS-level registry for apps and modules.

This directory is documentation-only for the platform layer concept. The Python package lives at `phistyle_platform.registry` to avoid colliding with Python's standard-library `platform` module.

Current scope:

- static app metadata;
- no auth;
- no agent runtime;
- no legacy app imports;
- no investment logic.
