# LLM Router

Architecture scaffold for routing AI work to model roles and providers.

Current status:

- Defines provider metadata.
- Defines model roles.
- Defines task routing policy.
- Returns local routing decisions only.

This module must not call real APIs or load real API keys yet.

