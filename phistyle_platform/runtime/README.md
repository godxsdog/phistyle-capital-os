# Agent Runtime

Python implementation of the PhiStyle OS agent runtime scaffold.

Current scope:

- register agents;
- list agents;
- run an agent manually;
- record in-memory run results;
- provide future scheduling and LLM Router integration points.

No real LLM APIs, background jobs, investment logic, or legacy app integrations are implemented.

